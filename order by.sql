-- order by
-- order by function gives the result with order by any coumn name
-- you can displlay the output orderd by last name or first name or asc/desc
-- sql automatically display the output in asc order
select * 
from employees
order by lastname asc,firstname desc;

-- more example on distinct and like
